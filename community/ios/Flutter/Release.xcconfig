#include? "Pods/Target Support Files/Pods-Runner/Pods-Runner.release.xcconfig"
#include "Generated.xcconfig"
// Deliberately not checked in. Release builds obtain ADMOB_IOS_APP_ID from
// Flutter/AdMob.xcconfig (ignored) or an Xcode/CI build setting; the Runner
// build phase rejects missing, malformed, and Google's test application IDs.
#include? "AdMob.xcconfig"
