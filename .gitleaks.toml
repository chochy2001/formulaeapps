[allowlist]
description = "Allowlist for known safe patterns"
paths = [
  '''(^|/)\.env\.example$''',
  '''(^|/)node_modules(/|$)''',
  '''(^|/)\.git(/|$)''',
  '''(^|/)vendor(/|$)''',
  '''(^|/)\.dart_tool(/|$)''',
  '''(^|/)build(/|$)''',
  '''(^|/)dist(/|$)''',
]
