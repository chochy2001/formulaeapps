# --- Public site config ---
# Inyectadas en build-time. Para usar en build local pásalas como vars de entorno
# o como --build-arg en `docker build`.
PUBLIC_SITE_URL=https://formulaeapps.com
PUBLIC_APP_URL=https://app.formulaeapps.com

# --- Analytics (opcional) ---
PUBLIC_GA_MEASUREMENT_ID=
PUBLIC_CLOUDFLARE_TOKEN=
